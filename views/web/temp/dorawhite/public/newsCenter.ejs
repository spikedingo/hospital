<div class="content-list-wrap">
    <div class="hospital-news container">
        <div class="innerBanner" style="display:none;">
            <img src="/themes/<%=staticforder%>/images/newsCenter.jpg" style="width:100%; margin:20px 0 0 0 ;">
        </div>

        <h2 class="page-intro">
            医院动态
        </h2>
        <h4>
            介绍本院的最新动态
        </h4>

        <div class="row areaPicNews">
            <% if(contentLists.length>0){ %>
            <% contentLists.forEach(function(item){ %>
                    <% include contentTemps/contentPost %>
                <% }) %>
            <% }else{ %>
                <li class="text-center">暂无推荐内容...</li>
            <% } %>

        </div>
    </div>
    <% include pagination %>
</div>